No installation required.

Dependencies:
 python >= 2.6, < 3
 gdata
 elementtree
 pytz

Oauth2 lib is included, as one from pip does not work with usosapi.

You may install needed python modules with:
$ pip install gdata elementtree pytz
